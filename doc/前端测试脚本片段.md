
# 前端测试脚本片段（Playwright）

> 说明：本文件提供“可直接照着补全”的 UI 自动化脚本片段（以 Vite dev 为例）。
> - 前端：`http://localhost:5173`
> - 后端：`http://localhost:8080`（Vite 已将 `/api` 代理到 8080）

## 0. 登录流程（captcha + JWT）
```ts
import { test, expect } from '@playwright/test'

test('login', async ({ page }) => {
  await page.goto('http://localhost:5173/login')

  // 1) 刷新验证码（按钮显示“刷新：XXXX”）
  await page.getByRole('button', { name: /刷新：/ }).click()
  const captchaLabel = await page.getByRole('button', { name: /刷新：/ }).textContent()
  const captcha = (captchaLabel || '').replace('刷新：', '').trim()
  await expect(page.getByRole('button', { name: /刷新：/ })).toBeVisible()

  // 2) 输入账号密码 + 填写验证码（默认账号：smile-admin / 123456）
  await page.getByLabel('用户名').fill('smile-admin')
  await page.getByLabel('密码').fill('123456')
  await page.getByLabel('验证码').fill(captcha)

  // 3) 登录
  await page.getByRole('button', { name: '登录并进入系统' }).click()
  await expect(page).toHaveURL(/\/dashboard$/)
})
```

## 1. 前端调用 GET /api/users 的交互流程验证
```ts
// Playwright snippet
import { test, expect } from '@playwright/test'
test('ui-flow', async ({ page }) => {
  await page.goto('http://localhost:5173/login')
  // TODO: 复用 login 流程（建议抽成 helper）

  await page.goto('http://localhost:5173/users')
  // 验证：页面有请求并渲染用户列表
  await expect(page.getByText('用户列表')).toBeVisible()
})
```

## 前端调用 POST /api/users 的交互流程验证
```ts
// Playwright snippet
import { test, expect } from '@playwright/test'
test('ui-flow', async ({ page }) => {
  await page.goto('http://localhost:5173/login')
  // TODO: 复用 login 流程

  await page.goto('http://localhost:5173/users')
  // TODO: 点击“新增用户”（需要权限 user:add）
  // TODO: 填写表单并保存
  // Verify request: POST /api/users (201)
})
```

## 前端调用 PUT /api/users/{id} 的交互流程验证
```ts
// Playwright snippet
import { test, expect } from '@playwright/test'
test('ui-flow', async ({ page }) => {
  await page.goto('http://localhost:5173/login')
  // TODO: 复用 login 流程

  await page.goto('http://localhost:5173/users')
  // TODO: 点击某行“编辑”（需要权限 user:edit）
  // TODO: 修改邮箱/手机号等并保存
  // Verify request: PUT /api/users/{id} (200)
})
```

## 前端调用 DELETE /api/users/{id} 的交互流程验证
```ts
// Playwright snippet
import { test, expect } from '@playwright/test'
test('ui-flow', async ({ page }) => {
  await page.goto('http://localhost:5173/login')
  // TODO: 复用 login 流程

  await page.goto('http://localhost:5173/users')
  // TODO: 点击某行“删除”（需要权限 user:delete）
  // TODO: 弹窗确认后会触发 prompt 输入密码，用于二次验证
  // Verify request: POST /api/auth/second-verify
  // Verify request: DELETE /api/users/{id} (204) with header X-Second-Verify
})
```

## 2. 组织树从后端加载验证
```ts
import { test, expect } from '@playwright/test'
test('org-tree', async ({ page }) => {
  await page.goto('http://localhost:5173/login')
  // TODO: 复用 login 流程

  await page.goto('http://localhost:5173/organizations')
  await expect(page.getByText('组织树')).toBeVisible()
  // Verify request: GET /api/orgs/tree (200)
})
```

## 3. 日志页从后端加载验证
```ts
import { test, expect } from '@playwright/test'
test('logs', async ({ page }) => {
  await page.goto('http://localhost:5173/login')
  // TODO: 复用 login 流程

  await page.goto('http://localhost:5173/security')
  await expect(page.getByText('登录日志')).toBeVisible()
  // Verify request: GET /api/logs/login (200)
  // Verify request: GET /api/logs/operations (200)
})
```
