
## 前端调用 GET /api/users 的交互流程验证
```ts
// Playwright snippet
import { test, expect } from '@playwright/test'
test('ui-flow', async ({ page }) => {
  await page.goto('http://localhost:5173')
  // TODO: complete UI actions
  // Verify request: GET /api/users
})
```

## 前端调用 POST /api/users 的交互流程验证
```ts
// Playwright snippet
import { test, expect } from '@playwright/test'
test('ui-flow', async ({ page }) => {
  await page.goto('http://localhost:5173')
  // TODO: complete UI actions
  // Verify request: POST /api/users
})
```

## 前端调用 PUT /api/users/{id} 的交互流程验证
```ts
// Playwright snippet
import { test, expect } from '@playwright/test'
test('ui-flow', async ({ page }) => {
  await page.goto('http://localhost:5173')
  // TODO: complete UI actions
  // Verify request: PUT /api/users/{id}
})
```

## 前端调用 DELETE /api/users/{id} 的交互流程验证
```ts
// Playwright snippet
import { test, expect } from '@playwright/test'
test('ui-flow', async ({ page }) => {
  await page.goto('http://localhost:5173')
  // TODO: complete UI actions
  // Verify request: DELETE /api/users/{id}
})
```
