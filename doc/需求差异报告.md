# 需求差异报告（按当前代码对齐）

- PRD: `/Users/smile/workspace/project/study/doc/prd.md`
- 对齐日期: 2026-03-11
- 结论: 当前实现已从“用户 CRUD 阶段”推进到“后端主干能力基本齐备、前端部分页面仍为原型占位”的状态。

## 总览

| 统计项 | 数量 |
|---|---:|
| PRD需求点总数 | 59 |
| FOUND（已具备） | 43 |
| PARTIAL（部分实现） | 15 |
| MISSING（未实现） | 1 |

## 模块差异明细

### 1) 用户管理

| 需求点 | 状态 | 代码证据 | 备注 |
|---|---|---|---|
| 用户增删改查 | FOUND | `UserController` 的 `GET/POST/PUT/DELETE /api/users` | 已接入鉴权与审计日志 |
| 启用/禁用 | FOUND | `sys_user.status` + 创建/更新接口 | 前后端均展示状态 |
| 批量导入/导出 | FOUND | `POST /api/users/import`、`GET /api/users/export` | 前端已有 API 封装，主界面未接入入口 |
| 用户名唯一 | FOUND | `validateUsernameUnique` + 数据库唯一索引 | |
| 加密密码 | FOUND | `BCryptPasswordEncoder` + `encodeIfNeeded` | 兼容旧明文并自动升级 |
| 姓名/手机/邮箱/头像字段 | FOUND | `sys_user` 字段 + DTO | |
| 单组织归属 | FOUND | `sys_user.org_id` | |
| 多角色分配 | FOUND | `sys_user_role` + `/api/users/{id}/roles` | 后端已完成 |
| 个人中心修改资料/密码 | PARTIAL | 通用用户更新与重置密码接口存在 | 缺少独立“个人中心”接口/页面 |
| 管理员重置密码 | FOUND | `POST /api/users/{id}/reset-password` | |

### 2) 组织管理（树形）

| 需求点 | 状态 | 代码证据 | 备注 |
|---|---|---|---|
| 组织增删改查 | PARTIAL | `OrgController` `POST/PUT/DELETE` + `/api/orgs/tree` | 后端已完成，前端页面仍以展示为主 |
| 拖拽排序 | PARTIAL | `sort_no` 字段 + 更新接口可写入排序值 | 缺拖拽交互与专用排序提交 |
| 设置父级组织 | PARTIAL | `parent_id` + 创建/更新接口 | 前端暂无完整编辑流 |
| 无限层级 | FOUND | 树构建 + `getDescendantOrgIds` | |
| 名称/编码/负责人/描述 | FOUND | `sys_org` 字段 + `Org` 实体 | |
| 节点下查看成员 | FOUND | `GET /api/orgs/{orgId}/users` | |
| 节点下添加/移除成员 | FOUND | `PUT/DELETE /api/orgs/{orgId}/users/{userId}` | |
| 组织成员双向关联 | FOUND | 组织查成员 + 用户 `org_id` 变更 | |

### 3) 角色管理（权限载体）

| 需求点 | 状态 | 代码证据 | 备注 |
|---|---|---|---|
| 创建角色 | PARTIAL | `POST /api/roles` | 后端已完成，前端交互未打通 |
| 角色启用/禁用 | PARTIAL | `Role.status` + `PUT /api/roles/{id}` | 前端未提供状态编辑 |
| 菜单/按钮权限绑定 | PARTIAL | `GET/PUT /api/roles/{id}/menus` | 前端页面目前偏静态展示 |
| 数据权限规则配置 | PARTIAL | `GET/PUT /api/roles/{id}/data-scopes` | 前端未接入真实编辑 |
| 角色赋予用户 | PARTIAL | `PUT /api/users/{id}/roles` | 前端未接入分配入口 |
| 多角色叠加 | FOUND | `sys_user_role` 多对多 | |

### 4) 权限管理

| 需求点 | 状态 | 代码证据 | 备注 |
|---|---|---|---|
| 菜单树管理（名称/路由/图标/父级/排序） | FOUND | `sys_menu` + `MenuController/MenuService` | |
| 按钮级权限标识 | FOUND | `perm_code` + `type=BUTTON` | |
| 前端按钮显隐控制 | PARTIAL | `UsersView` 使用 `hasPerm('user:xxx')` | 仅部分页面使用 |
| 数据权限预设（全部/本部门及子部门/本部门/本人） | FOUND | `RoleDataScope` + `DataScopeService.Scope` | |
| 按模块配置规则 | PARTIAL | `module_code` 已支持，但当前主要用于 `USER` | 订单/报表仅示例未落地 |
| 查询按规则动态收敛 | PARTIAL | `UserServiceImpl` 中按 scope 限制查询 | 未采用统一拦截器机制 |

### 5) 系统支撑

| 需求点 | 状态 | 代码证据 | 备注 |
|---|---|---|---|
| 用户名密码登录 | FOUND | `/api/auth/login` | |
| 验证码防刷 | FOUND | `/api/auth/captcha` + 校验 | |
| JWT 会话管理 | FOUND | `JwtTokenService` + `JwtAuthFilter` | |
| 前端登录后加载权限并动态菜单/按钮 | PARTIAL | 登录写入 `menus/permissions`，`AppLayout` 动态菜单 | 仅部分页面按钮受权限控制 |
| 后端注解鉴权 | FOUND | `@PreAuthorize` + `@EnableMethodSecurity` | |
| 登录日志 | FOUND | `sys_login_log` + `/api/logs/login` | |
| 关键操作日志 | FOUND | `AuditService.opLog` + `/api/logs/operations` | |
| BCrypt | FOUND | `SecurityConfig.passwordEncoder` | |
| 敏感操作二次验证 | PARTIAL | `/api/auth/second-verify` + 删除用户/组织需 `X-Second-Verify` | 覆盖面可继续扩展 |

### 6) 非功能与验收项

| 需求点 | 状态 | 说明 |
|---|---|---|
| 可维护性（模块边界清晰） | FOUND | 后端已形成 controller/service/mapper/security 分层 |
| 安全性（前后端双保险） | PARTIAL | 后端鉴权较完整，前端仅部分页面做按钮级控制 |
| 可审计性（关键操作留痕） | PARTIAL | 关键操作已有审计日志，但尚未形成“全链路”覆盖清单 |
| 核心流程可用（用户/组织/角色/菜单/数据权限） | PARTIAL | 后端主链路已可用，前端组织/角色权限仍有交互缺口 |
| 菜单与按钮显隐与权限一致 | PARTIAL | 菜单已动态，按钮仅部分页面接入 |
| 数据权限按模块生效 | PARTIAL | USER 模块可用，更多业务模块待接入 |
| 登录鉴权日志与安全策略可验证 | FOUND | 登录、JWT、验证码、日志、二次验证均可演示 |

## 唯一明确缺失项（MISSING）

1. 订单模块等非 USER 业务模块的数据权限实际接入尚未开始（目前仅有 USER 模块落地与示例文案）。

## 优先补齐建议（按影响排序）

1. 打通前端“组织管理、角色权限”真实编辑流程（替换当前展示型页面）。
2. 将按钮级权限控制扩展到全部操作入口。
3. 扩展二次验证覆盖范围（如角色/菜单删除、权限绑定变更）。
4. 将数据权限机制从 USER 模块扩展到更多业务模块。
