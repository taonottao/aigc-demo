
## 联调验证：DELETE /api/users/{id}
- 请求: DELETE /api/users/{id}
- 检查点1: 前端发出的参数结构是否符合接口约定
- 检查点2: 后端返回状态码与字段是否符合预期
- 检查点3: 页面展示与错误提示是否与返回一致

## 联调验证：DELETE /api/users/{id}
- 请求: DELETE /api/users/{id}
- 检查点1: 前端发出的参数结构是否符合接口约定
- 检查点2: 后端返回状态码与字段是否符合预期
- 检查点3: 页面展示与错误提示是否与返回一致
